version: "3.9"

services:
  frontend:
    build: .
    container_name: my-frontend-app
    ports:
      - "3000:80"  # host port 3000 -> container port 80
    environment:
      NODE_ENV: production
    restart: unless-stopped
